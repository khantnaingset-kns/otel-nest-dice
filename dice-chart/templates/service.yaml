apiVersion: v1
kind: Service
metadata:
  name: {{ include "dice-chart.fullname" . }}
  labels:
    {{- include "dice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dice-chart.selectorLabels" . | nindent 4 }}
